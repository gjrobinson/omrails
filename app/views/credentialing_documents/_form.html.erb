  <%= form_for(credentialing_document) do |f| %>
    <% if credentialing_document.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(credentialing_document.errors.count, "error") %> prohibited this credentialing_document from being saved:</h2>

        <ul>
        <% credentialing_document.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :filename %>
      <%= f.text_field :filename %>
    </div>

    <div class="field">
      <%= f.label :content_type %>
      <%= f.text_field :content_type %>
    </div>

    <div class="field">
      <%= f.label :file_contents %>
      <%= f.text_field :file_contents %>
    </div>

    <div class="actions">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  <% end %>

